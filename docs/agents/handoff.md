# Handoff — mvp-reto-vivienda (Hackathon Colsubsidio × 30X)

> Memoria de sesión + roadmap del **build**. Leer al inicio, actualizar al final.
> El roadmap es un DAG: una tarea está **ready** solo cuando sus dependencias están done.
> **Este repo es el canon de los docs vivos** (este handoff + `docs/URGENTE-Y-NOTICIAS.md`).
> Repo hermano `plan-research` = archivo histórico de investigación y datos crudos.

## Memory

_Estado actual del trabajo. Lo nuevo arriba. Mantener corto; enlazar a ADRs/commits en vez de duplicar._
_Convención: toda actualización que toque un track (A/B/C/D) lleva **fecha y hora**, no solo fecha — con 4 personas tocando ramas distintas el mismo día, la hora es lo que evita pisarse._

- 2026-07-24 18:50 (hora Colombia) — **🎨 El design system de Colsubsidio reemplaza a "El formato sellado", y la consola del asesor se porta al kit `advisor-panel` (rama `feat/consola-asesor`).** Pedido de Alejandro con el zip del design system en mano. **173/173 tests, typecheck, lint limpios, las 3 rutas del asesor en 200.** Registrado en el [ADR 0004](../adr/0004-design-system-colsubsidio.md).
  - **El diagnóstico que decidió todo:** el repo tenía **dos** sistemas de diseño para el mismo producto, y el equipo ya había empezado a portar uno sin que quedara escrito. `app/chat.css` declara en su encabezado ser el puerto de `ui_kits/lead-chat/`, `public/marca/` ya tenía los logos del zip y `globals.css` ya usaba su `data-theme`. **La superficie del lead estaba portada; la del asesor no.** Y el zip traía justo el kit hermano, `ui_kits/advisor-panel/`. Esto no fue elegir un diseño nuevo: fue terminar una migración a medias.
  - **Sobre v0:** se revisaron los 24 templates de la categoría *Dashboards*. Los cercanos (*Pulse — Incident Response Console*, *SalesOps*) son shadcn + Radix + Recharts + client components; importar uno habría reescrito una app que hoy renderiza **sin JS de cliente**. Se tomó el **arquetipo** (consola lista/detalle), no el código — y ese arquetipo ya venía instanciado en marca dentro del propio zip.
  - **Qué cambió:** fuentes a Sora + Work Sans + JetBrains Mono (**toca también chat y landing**: viven en `layout.tsx`); radios 3/6 → 6/10/16/píldora; **entran sombras**; paleta de estados separada de la de marca; violeta para dato simulado; `lucide-react` en vez de `→ ✓ ✗` a mano. `/asesor` y `/asesor/[leadId]` comparten `ListaLeads` bajo un shell nuevo (`app/asesor/layout.tsx`) con la barra lateral y el lockup — con eso muere el riel azul que estaba **duplicado literal** entre dos archivos.
  - **`globals.css` quedó en tres capas y solo se edita la primera** (los tokens del kit, con sus nombres verbatim, para que el CSS de un ui_kit se pegue sin traducir). La capa 2 son los tokens viejos (`--papel`, `--tinta`, `--campo`) como **alias**, para que los ~20 componentes que los usan como clases sigan compilando mientras se migran.
  - **Tres cosas que se corrigieron del zip, no se copiaron a ciegas.** (1) **Los rótulos salen del repo:** el kit dice "Listo · cupo restringido", aquí sigue diciendo **"Listo · cupo 90/10"**, que es la regla que el asesor valida. (2) **Tres tokens de estado no pasaban AA** con la tinta que el propio kit especifica (nutrición 3,26:1 en claro; verde y ámbar 3,49:1 y 3,25:1 en oscuro): se conservó el matiz y se ajustó la luminosidad. (3) El kit resuelve búsqueda y filtro con `useState`; aquí van por **querystring con `<form method="get">`**, para no volver cliente la consola.
  - **Un bug que ni el typecheck ni los tests atrapaban:** los alias apuntaban `--r-sm` a `var(--radius-sm)`, que apunta de vuelta a `var(--r-sm)`. Ciclo → el navegador descarta la propiedad → **todas las esquinas de la app a cero**. Se vio sirviendo el CSS compilado del dev server, no leyendo el archivo. Si tocas la capa de alias, verifica el CSS servido.
  - **Un test se modificó, y conviene saber cuál:** `TablaFactores.test.tsx` fijaba el glifo literal `"✗ No"`, y el kit prohíbe la iconografía unicode a mano. La aserción pasa a **contar los marcadores renderizados**, que es el contrato que el test dice proteger ("no se esconden") y sobrevive al próximo cambio de piel. Los demás 172 pasaron intactos.
  - **⚠️ Pendiente de verificación humana:** el recorrido a ojo no se hizo (la automatización de navegador no estuvo disponible en esta sesión). Sí se verificó contra el server real: 3 rutas en 200, la ficha renderizando sus 7 factores con los 4 campos cada uno, 1 solo `.resaltado` por pantalla, y las utilidades resolviendo los tokens (`rounded-sm` → 6px, `shadow-xs`, `bg-estado-*-bg`, `.rotulo`). **Falta mirarlo en pantalla en claro y oscuro, y a 375px.**
  - **⚠️ Deuda conocida:** la landing (`components/landing/`) quedó fuera del alcance acordado y **usa emoji**, que el design system prohíbe. Y `BloqueProyectos` no pinta nada cuando el lead trae 0 proyectos — es comportamiento preexistente, pero con el catálogo real Carlos tiene 0, así que su ficha no muestra sección de proyectos.
- 2026-07-24 17:35 (hora Colombia) — **🔀 `main` mergeado en `frontend-chat`: la conversación nueva ya corre dentro del escenario de marca.** Las dos ramas tocaron `ChatWhatsApp.tsx` con propósitos distintos —`main` reescribió la **lógica** (`preguntas.ts`, entradas 16:45 y 17:20), `frontend-chat` reescribió el **mundo visual** (`app/chat.css`, el escenario, `SelloPerfil`, `BotonTema`)— y el auto-merge trajo toda la lógica intacta pero dejó el **pie del chat** roto. **161/161 tests, typecheck y lint limpios, `/` en 200.**
  - **Lo que estaba roto y no era obvio:** el conflicto marcado era solo el cierre de los `</div>`; el daño real estaba **arriba del marcador**, donde git auto-mergeó sin avisar. Nuestro footer seguía ramificando por `pasoActual.tipo` (`"texto" | "si_no"`, campo que `main` **eliminó**) y llamando `responderPregunta("Sí", true)` cuando el segundo argumento ahora es un `Respuesta { patch, acuse }`. Un conflicto de git limpio no significa un merge correcto: lo que lo delató fue `npx tsc --noEmit`, no el marcador.
  - **El pie quedó en el híbrido de verdad** (spec 02 D4): una sola rama para `fase === "pregunta"`, con los chips (`pasoActual.opciones`) **encima** del campo de texto, y el campo **siempre presente** con el `placeholder` del paso. Ingreso y zona salen sin chips gratis, sin caso especial: llegan sin `opciones` y la fila no se pinta. Clases `.chips` / `.chip` nuevas en [`chat.css`](../../app/chat.css) sobre los tokens `--chat-quick-*` que ya existían — `.qr`/`.btn` no servían porque se reparten la fila a partes iguales y no envuelven.
  - **Tres desalineaciones de diseño que dejó el cambio de lógica, ya cerradas:** (1) el botón de consentimiento decía *"Sí, acepto"* pero la burbuja del usuario escribía *"Sí, la comparto"* — ahora dicen lo mismo; (2) `SelloPerfil` pintaba `situacion_crediticia` **cruda**, así que el lead leía **`sin_info`** en su propio sello — se mapea a etiquetas humanas (`Al día`, `En proceso de arreglo`, `Con algo pendiente`, `Por confirmar`), ninguna con tono de veredicto; (3) la cabecera decía "Colsubsidio Vivienda" mientras el agente se presentaba como **Sara** — ahora lee `NOMBRE_AGENTE`, no un literal.
  - **🔴 Ojo, esto se está dando por hecho y NO existe: la conversación no se guarda en Supabase.** [`app/page.tsx`](../../app/page.tsx) sigue con `console.log("Lead terminado:", lead)`. Toda la infraestructura está construida y **sin usar**: `POST /api/leads` → `guardarLeadCurado()` ([`lib/leads-repo.ts`](../../lib/leads-repo.ts)), y la tabla `conversaciones` de [`db/schema.sql`](../../db/schema.sql) (una fila por mensaje) no la escribe nadie. Cablearlo es el ticket [006](../tasks/006-orquestador.md), que sigue **todo**.
  - **Pendiente de verificación humana:** el recorrido a pie del chat no se hizo (la automatización del navegador no estaba disponible en esta sesión). Lo que hay que mirar en pantalla: que chips y campo de texto convivan, que responder **escribiendo** valga lo mismo que tocar el chip, y que los chips se lean en tema claro y oscuro.
- 2026-07-24 18:10 (hora Colombia) — **🔌 Cadena conectada (ticket [006](../tasks/006-orquestador.md), costura S4) + la razón real de que NADA se guardara en Supabase.** Mani corrió dos conversaciones y no apareció ninguna fila. La causa no era el chat: **la tabla `leads` de producción no tiene la columna `puntaje`**. Está en `db/schema.sql` desde que el tablero metió el puntaje 0–100, pero la base se creó antes y nunca se migró, así que todo upsert rebotaba con `Could not find the 'puntaje' column`. Se diagnosticó corriendo `/api/curar` contra la base real y confirmando por el cliente de Supabase que la columna no existe. **169 tests + 12 nuevos = 173 verdes, typecheck, lint y build limpios.**
  - **🔴 ACCIÓN PENDIENTE (quien tenga la consola de Supabase):** correr [`db/migracion-001-puntaje.sql`](../../db/migracion-001-puntaje.sql). Idempotente, no borra datos. Agrega la columna, **recrea la vista `cola_asesor`** (un `select l.*` se expande al crearla: sin recrearla la cola seguiría ordenando con ceros) y relaja el CHECK de proyectos.
  - **Qué se construyó:** [`/api/curar`](../../app/api/curar/route.ts) encadena conversación → motor → matcher → explicación → DB. La lógica vive en [`lib/curar.ts`](../../lib/curar.ts) (puro, testeable sin server) y la ruta queda en HTTP + persistencia. Nuevo [`lib/scoring/capacidad.ts`](../../lib/scoring/capacidad.ts) (parte del ticket [004](../tasks/004-capacidad-compartida.md)): el precio máximo es el gate del 40% despejado al revés, y hay test de que en ese precio la cuota da exactamente 40% — si se desincroniza, el matcher recomendaría lo que el gate rechaza.
  - **También se guarda el hilo** (tabla `conversaciones`, que estaba vacía para todo lead nuevo): una fila por mensaje, con filas `sistema` para la ingesta y para el consentimiento con su hora. **Sin autorización no se persiste nada** (403).
  - **El porqué se redacta sin LLM**, con los `valor` de los factores que el motor ya calculó: no puede inventar cifras y el cierre de la cadena no depende de que Gemini esté vivo. `/api/explicacion` sigue para la versión pulida en la ficha.
  - **Los 3 canónicos contra el catálogo REAL:** Diana `listo` 75/100 · 3 proyectos (LA MACARENA, MONGUI, LA ARBOLEDA) · Carlos `listo_restriccion_cupo` 57/100 · **0 proyectos** (los 18 con el cupo 90/10 agotado: la munición del pitch sale sola) · Yuliana `nutricion` 0 con su trigger.
  - **⚠️ Dos cosas que hay que saber antes del video.** (1) Correr una conversación **pisa** la fila sembrada de ese personaje (upsert por `lead_id`), que es lo que pide el ticket pero cambia la ficha curada a mano por la calculada. (2) El CHECK de proyectos pasó de `0 ó 2-3` a `≤ 3`: rechazaba exactamente 1 y con eso **se perdía el lead entero**, lo que choca con "nadie se descarta" ([ADR 0003](../adr/0003-esquema-db-leads.md), enmienda). **Es un cambio a un criterio de aceptación: el TEAM lo ratifica o lo revierte.**
  - **Pendiente, no bloqueante:** que la cadena corra en la URL pública (ticket [009](../tasks/009-deploy-verificado.md)) exige la migración primero; y el proyecto de entrada de los 3 canónicos sigue sin existir en el catálogo real (ticket [001](../tasks/001-personajes-canonicos.md)), así que hoy se califican contra el proyecto más económico.
- 2026-07-24 17:20 (hora Colombia) — **🙅 El agente ya no le recita al lead su propia ficha** (mismo Track A, sobre `main`). Mani lo vio en pantalla y no le gustó: el mensaje decía *"ya sé que eres afiliada a Colsubsidio, estás en Bogotá y tu hogar tiene ingresos entre 3 y 5 SMMLV"*. Suena a expediente y asusta justo donde hay que generar confianza — y el ingreso es el dato que más incomoda oír de vuelta.
  - **Cómo quedó:** el mensaje dice que sus datos ya están y **que no le vamos a hacer repetir nada**, y **usa** la ciudad en vez de recitarla (*"empiezo por buscarte opciones en Bogotá"*). El ingreso y la afiliación **no se mencionan nunca**. Cuatro tests nuevos lo blindan, incluido uno que falla si el rango de ingreso vuelve a colarse en ese mensaje.
  - **El criterio de aceptación 1 se sigue cumpliendo**, y esto está escrito en [`spec.md §5`](../spec.md): lo que exige es que el lead **sepa** que no le repreguntarán, no que le lean su ficha. La ficha completa la ve el asesor, que es para quien es. También se blindó el system prompt de `/api/chat`, que ahora tiene prohibido recitarle al lead datos suyos que el mensaje base no traiga.
  - **Narrados alineados** ([`diagramas/02-conversador.md`](../specs/diagramas/02-conversador.md) y [`diagramas/00-mvp-unificado.md`](../specs/diagramas/00-mvp-unificado.md), que entraron a `main` mientras esto se hacía): el narrado citaba textualmente el saludo viejo. **Los mermaid y los PNG NO se tocaron** — el nodo del diagrama sigue etiquetado *"Esto ya lo sabemos de ti"*, que como nombre de paso sigue siendo cierto (el sistema sí lo sabe; lo que ya no hace es recitarlo). Se dejó así a propósito para no invalidar los PNG exportados para la reunión; quien los regenere puede renombrarlo a *"No te voy a hacer repetir"* en los **dos** archivos (`check_diagramas.py` lo exige).
  - **⚠️ Para Rol 4 (video):** el guion decía *"y se lo dice"* en el beat 1. Ya está anotado — la voz en off puede nombrar afiliación/ciudad/ingreso porque le habla al jurado, pero **la pantalla del chat no los muestra**; quien los enumera es la ficha del asesor en el beat 3.
- 2026-07-24 16:45 (hora Colombia) — **💬 La conversación de WhatsApp dejó de ser una encuesta (Track A, rama `feat/conversacion-humana`).** Pedido de Mani: que suene humana, porque lo que se está vendiendo es *"algo que haces una vez en tu vida y probablemente al lado de otra persona"* (mentor). Se reescribieron [`lib/conversacion/preguntas.ts`](../../lib/conversacion/preguntas.ts), [`ChatWhatsApp.tsx`](../../components/chat/ChatWhatsApp.tsx) y el system prompt de [`/api/chat`](../../app/api/chat/route.ts). **166/166 tests verdes (23 nuevos), typecheck y lint limpios, `/` en 200.**
  - **Cuatro cambios de fondo, no de maquillaje.** (1) **Cada pregunta dice para qué sirve antes de preguntar** — el ingreso ya no es "¿cuál es tu rango de ingresos?" sino "solo la uso para no mostrarte casas que después te aprieten el bolsillo". (2) **Cada respuesta recibe un acuse** ("¡La primera! 🎉 Eso te deja el camino despejado para los subsidios…"): sin reacción, un chat es un formulario con burbujas. Los acuses **no pasan por el LLM** — son instantáneos, así que humanizan sin costar latencia. (3) **El agente tiene nombre (Sara)** y saluda con el proyecto por el que entró el lead. (4) **Orden nuevo:** primero lo que ilusiona (¿primera vivienda?), después lo incómodo (ingreso, crédito).
  - **Híbrido de verdad (D4 del [spec 02](../specs/02-conversador.md), cerrado por el mentor): el input libre ya nunca desaparece.** Antes cada paso era *o* botones *o* texto; ahora los chips conviven con el input en todos los pasos y escribir vale lo mismo que tocar el chip (mismo `Respuesta { patch, acuse }`). Ingreso y zona siguen **sin chips** a propósito: ahí la lista sesga.
  - **Cerró 2 de las 3 brechas de datos que reportó el spec 02 ayer.** `situacion_crediticia` ahora sale como el **enum** que espera el motor (chips que llevan el valor + normalización del texto libre). El **ingreso ya se obtiene como número**: `parsearIngresoMensual()` entiende "4.500.000", "1'500.000", "2 millones y medio", "3 salarios mínimos", "entre 2 y 3"; y a quien ya trajo rango del enriquecimiento se le toma el **punto medio** sin repreguntarle (criterio 1 intacto). Si la frase es ambigua ("depende del mes") **no se adivina**: se guarda el texto crudo y ya. **Sigue abierta la brecha del `subsidio_monto_mensual`** — nadie pregunta el monto, así que el subsidio todavía no baja la cuota.
  - **⚠️ Dos cosas que el TEAM debe ratificar o tumbar** (son las preguntas 3 y 6 del spec 02, respondidas provisionalmente para no dejar el motor roto): el **orden** nuevo de las preguntas, y que el **punto medio del rango** sea aceptable como ingreso de quien ya venía perfilado. Ambas se revierten en una línea.
  - **NO se tocó:** quién conduce la conversación (sigue determinista, D1-A — la opción B no está ratificada), el scoring, el matcher, ni la pregunta de afiliación (nodo 4, sigue sin existir porque es decisión abierta del TEAM). El fallback de 3s del LLM quedó igual.
  - **Barrido de documentación en la misma rama** (para que ningún doc siga describiendo el chat viejo): [`URGENTE-Y-NOTICIAS.md`](../URGENTE-Y-NOTICIAS.md) (entrada nueva + el 🔴 de las brechas anotado), [`spec.md §6`](../spec.md) (cómo se responde cada pregunta + el contrato de tono), [`specs/02`](../specs/02-conversador.md) y [`specs/00`](../specs/00-mvp-unificado.md) (brechas y orden), [`PRODUCT.md`](../../PRODUCT.md) (principio 5 nuevo: *conversar, no encuestar*), [`AGENTS.md`](../../AGENTS.md) (convención durable, porque esto se revierte solo si nadie lo escribe), [`reparto-inicial.md`](../reparto-inicial.md) (el snippet de contratos estaba desactualizado desde antes: `situacion_crediticia` como `string` y `FactorScore` sin los campos del puntaje), [`context.md`](context.md) (dos términos: *acuse*, *atajo*), [`mvp-layout.md`](../mvp-layout.md) (decisión 5 ampliada), [`guion-video.md`](../pitch/guion-video.md) (**nota de grabación nueva: al menos una respuesta hay que escribirla en cámara**, si no el video muestra justo el chatbot de botones que el mentor rechazó) y los tickets [005](../tasks/005-agendador.md), [006](../tasks/006-orquestador.md) y [011](../tasks/011-test-criterio-1.md).
- 2026-07-24 16:10 (hora Colombia) — **📋 Paquete de specs por componente + diagramas mermaid, y digest de la charla con el mentor.** No tocó código. Nueve documentos nuevos: [`docs/reto/charla-mentor.md`](../reto/charla-mentor.md) (digest curado, el transcript crudo se queda fuera del repo público) y [`docs/specs/`](../specs/README.md) — un spec por componente (ingesta · conversador · scoring · match+agenda · nutrición · dashboard) más el [unificado](../specs/00-mvp-unificado.md), cada uno con su mermaid. **Los 7 diagramas validados renderizando con `@mermaid-js/mermaid-cli`**, no a ojo.
  - **Es un borrador para que el TEAM decida, no decisiones tomadas.** Cada spec separa el **QUÉ** (contrato de comportamiento, con fuente citada) del **CÓMO** (diseño propuesto, discutible), marca cada decisión como `[CERRADA — fuente]` / `[HOY — así está construido]` / `[PROPUESTA — TEAM decide]`, y cierra con una sección `## Preguntas al TEAM`. Nada se resuelve por omisión. Convenciones en [`docs/specs/README.md`](../specs/README.md).
  - **🔴 Brecha grande que salió al escribir el spec 02, nadie la había visto:** el motor necesita `ingreso_hogar_mensual` (**número**) para el gate del 40%, pero la conversación solo pregunta `rango_ingreso_hogar` (**texto libre**) y **nadie convierte lo uno en lo otro**. Consecuencia: **cualquier lead que entre por "soy yo" cae a nutrición siempre**, gane lo que gane. Mismo patrón en `subsidio_monto_mensual` (nunca se pregunta → el subsidio jamás baja la cuota) y en `situacion_crediticia` (se pregunta en texto libre donde el tipo espera un enum). Detalle en [spec 02, "Estado hoy vs contrato"](../specs/02-conversador.md).
  - **Otras tensiones nombradas, ninguna resuelta por mí:** las **dos escalas de puntaje** conviviendo (`Score.puntaje` del motor vs. `calcularPuntaje()` de `puntaje.ts`, que es el que ve toda la UI — y los pesos ni siquiera coinciden en el orden); los **IDs de `slots.json`** (`p-03`…) que no existen en el catálogo real, así que las franjas no van a aparecer para proyectos reales; el techo del puntaje que hoy es **90** (similitud fija en 0,5) y **80 para no afiliados**; y qué significa honestamente que el agente "aprenda" (propuesta: grounding actualizable + recalibración offline con autor y fecha; **nunca** ajuste automático en línea, que sería caja negra).
  - **Docs actualizados en la misma tanda:** [`mvp-layout.md §3`](../mvp-layout.md) marcado SUPERSEDED (el strawman se queda como registro), [`spec.md §7`](../spec.md) con la evidencia del mentor anotada en los supuestos de cédula y multi-canal (**sin marcar el checkbox — eso lo hace el TEAM**), y [`context.md`](context.md) con 10 términos nuevos de la operación real (separación, escrituración, click-to-WhatsApp, atribución de canal, propenso/no propenso, handoff a humano, slot-filling, guardrail, grounding actualizable, recalibración offline) + "códigos griegos" corregido, que seguía diciendo "decisión abierta" cuando ya se cerró.
  - **Lo que sigue: la reunión.** Orden sugerido de lectura: [00 unificado](../specs/00-mvp-unificado.md) para el mapa → [02 conversador](../specs/02-conversador.md), que es donde está el grueso de lo que hay que definir → el resto por rol.
- 2026-07-24 15:20 (hora Colombia) — **🟢 Rol 2 (Datos & Motor): cerrada la doble fuente de puntaje — el asesor veía un número que el motor nunca calculó.** Al mergear, quedaron **dos** cálculos de puntaje vivos a la vez: el mío en `Score.puntaje` (entrada 11:45, dentro de `calcularScore()`, continuo, con pesos de `config.ts`) y [`lib/scoring/puntaje.ts`](../../lib/scoring/puntaje.ts) (entrada 13:00, del tablero, binario sobre `factor.cumple`, pesos propios que no coincidían ni en el orden). La UI (`FichaLead.tsx`, `FilaLeadPuntaje.tsx`, `lib/tablero/agrupadores.ts`) llamaba **solo** al segundo — `Score.puntaje` se calculaba y se guardaba en la DB (`db/schema.sql`) pero nadie lo mostraba. Lo encontró Daniel leyendo el diff, no un test: los tests de `puntaje.ts` eran auto-consistentes (comparaban su propio cálculo contra sí mismo) así que nunca iban a fallar por esto.
  - **Por qué el binario era peor, no solo distinto:** en `puntaje.ts`, `cuota_ingreso_40` (35 de 100 puntos, el mayor peso) y `cupo_90_10` (10 puntos) leen `factor.cumple`, que mi motor deja **siempre `true`** para cualquier lead que ya pasó el gate (por construcción) o que nunca bloquea (cupo es señal, no corte). Resultado: 45 de 100 puntos posibles no diferenciaban entre NINGÚN lead "listo" — la holgura real (cuánto margen bajo el 40% tiene cada quien) no llegaba a pesar nada en el número que ordenaba el tablero.
  - **Arreglo: una sola fuente.** Se borró `lib/scoring/puntaje.ts` y su test. `agrupadores.ts::puntajeDe`, `FilaLeadPuntaje.tsx` y `FichaLead.tsx` leen `curado.score.puntaje` directo. `TablaPuntaje.tsx` se reescribió para pintar `FactorScore[]` (peso/valor_norm/aporte) en vez del shape `Puntaje` viejo — ya no dice "Cumple/No cumple" (binario), dice `peso% × señal%` porque ahora el aporte es continuo.
  - **Verificado:** `npx tsc --noEmit` limpio, **134/134 tests** (147 con los 13 que traía el motor − los ~13 de `puntaje.test.ts` que se borraron por obsoletos), `npm run lint` limpio (el único error que queda, `components/ui/BotonTema.tsx`, es preexistente y de otra rama — no se tocó).
  - **Lección para el resto del equipo:** si dos personas tocan "cómo se calcula/muestra el puntaje" en paralelo sin leer `lib/scoring/index.ts` primero, se repite. `Score.puntaje` (con `factores[].peso/valor_norm/aporte`) es ahora el ÚNICO cálculo de puntaje del repo — cualquier pantalla nueva que quiera mostrarlo lee esos campos, no reinventa pesos propios.
- 2026-07-24 13:50 (hora Colombia) — **🟢 Rol 3 (Calidad IA & Demo): catálogo real cableado en producción + fallback del experto (ticket 010).** Rama `rol3-calidad-ia` (no tocó `main` aún), commit `900d06b`. **94/94 tests, typecheck y lint verdes.**
  - **Swap del catálogo (decisión de Mani: "el MVP debe servir con datos reales, sin exponerlos a git").** El matcher y `/api/match` + `/api/explicacion` pasan del catálogo inventado de [`lib/matching/fixtures.ts`](../../lib/matching/fixtures.ts) al REAL de los 18 proyectos ([`data/sintetica/proyectos.json`](../../data/sintetica/proyectos.json)) vía nuevo [`lib/matching/catalogo.ts`](../../lib/matching/catalogo.ts) (mapea null→undefined, ignora campos extra). `fixtures.ts` queda como **catálogo controlado solo para los tests** del matcher (la trampa de "Ciudadela del Este").
  - **Regla 90/10: se conserva DURA, alineada con el tablero.** Al integrar apareció un choque: yo había suavizado el matcher (no-afiliado sin cupo → recibe proyectos marcados "sin cupo") pero el tablero ya mergeado ([`lib/fixtures/cola-historica.ts`](../../lib/fixtures/cola-historica.ts) + su test) construyó a propósito lo **opuesto**: no-afiliado sin cupo → **0 proyectos**, y ese vacío ES la munición del reto (16/16 proyectos ya venden por encima del 10%). No coexisten. **Mani decidió alinear con el tablero:** revertí mi suavizado, el matcher vuelve a descartar por `cupoLibre > 0`. El comentario de `matchear()` ahora deja explícito por qué el vacío no se esconde. **Nota:** el Carlos canónico del demo no se ve afectado por esto — su ficha lee `LeadCurado` guardado (sus 2 proyectos), no el matcher en vivo; el 0 solo aplica al flujo "soy yo" y a los leads sintéticos del tablero.
  - **🔴 Costura abierta para el Integrador (ticket [001](../tasks/001-personajes-canonicos.md), NO es de Rol 3):** los 3 personajes canónicos siguen con `proyecto_interes` y ciudad **inventados** (Diana→Torres de Bellavista/Bogotá, Carlos→Reserva del Poblado/**Medellín**, Yuliana→Alameda del Río). El catálogo real **no tiene Medellín**. Hoy no rompe el demo (las fichas canónicas usan fixtures guardadas), pero en cuanto 006 conecte la cadena en vivo, alinear los personajes al catálogo real (001) es lo que da coherencia. Cascada al tocarlo: `lib/fixtures/{leads-evento,leads,scores,leads-curados}.ts` + `docs/explicaciones-referencia.md`.
  - **Ticket 010 — fallback determinístico del experto (mitad de `/api/explicacion`).** Si el LLM cae al conectarse (el cold-start de Vertex que tira 500 a ~6s) o no hay credencial, la ruta sirve la explicación de referencia del personaje ([`lib/fixtures/leads-curados.ts`](../../lib/fixtures/leads-curados.ts), leída sin duplicar) en vez del 500/503 mudo. Nuevo [`lib/matching/explicacion-fallback.ts`](../../lib/matching/explicacion-fallback.ts) + test. Solo aplica a los 3 pre-sembrados y a `tipo=global` (la ficha del asesor); el "soy yo" degrada honesto (null → 503 con diagnóstico). **No miente:** nada afirma que el texto lo generó el modelo. **Pendiente del Integrador (006):** el cliente que consuma `/api/explicacion` aún debe cortar por timeout si el LLM se **cuelga** (no solo si falla), igual que el conversador en [`ChatWhatsApp.tsx`](../../components/chat/ChatWhatsApp.tsx); el fallback de servidor solo cubre el fallo, no el cuelgue.
  - **Bloqueado, sin hacer (no depende de Rol 3):** ticket [018](../tasks/018-similitud-en-explicacion.md) (similitud) espera que Rol 2 haga [016](../tasks/016-distribuciones-por-proyecto.md) — `distribuciones.json` sigue **global**, no por proyecto. Evaluar el output real de `/api/explicacion` (item 3 del rol) necesita `GEMINI_API_KEY` local, que no existe en esta sesión. Quitar la fixture de `precio_maximo` espera el ticket [002](../tasks/002-contratos-capacidad-en-score.md) del Integrador. QA sin narración de los 3 personajes = sábado tras la integración [014](../tasks/014-recorrido-criterio-4.md).
- 2026-07-24 12:20 (hora Colombia) — **🟢 Seguridad cerrada por Rol 4 (Pitch & Video): los dos leaks de data real tapados (tickets [021](../tasks/021-plan-research-privado.md) + [022](../tasks/022-sanear-buyer-personas-md.md)).** Rama `rol4-cierre` (no tocó `main` aún).
  - **021 — `plan-research` ahora es PRIVATE.** `gh repo edit hackathon-30x-colsubsidio/plan-research --visibility private`, verificado `isPrivate: true`. Los 3 archivos reales (`hackathon_VIVIENDAv2.xlsx`, `Links brochures .xlsx`, `Buyer Person.pptx`, commit `8bc42eb3`) ya **no son descargables sin login**. Reescribir el historial para borrarlos queda post-hackathon; riesgo residual = quien clonó antes.
  - **022 — `data/buyer-personas-vivienda.md` saneado en ESTE repo público.** Removidas las **22 tablas de "Top empresas"** (nombres reales de empleadores + conteos de compradores: TEXTILES DE TOCANCIPA, ALPINA, ADECCO…). Se **conservan los agregados %** (afiliación/salario/edad/segmento por proyecto) que alimentan el [ticket 016](../tasks/016-distribuciones-por-proyecto.md). El md crudo con nombres se guardó local en `docs/recursos-reto/buyer-personas-vivienda-RAW.md` (gitignored) para no perderlo.
  - **`.gitignore` blindado:** una transcripción `.md` de un insumo esquivaba las reglas `*.pptx`/`*.xlsx`. Ahora `data/*.md` se ignora por defecto con excepción explícita para el archivo saneado; verificado con `git check-ignore`.
  - **Falta (acción de Rol 2):** confirmar que el `buyer_personas.json` derivado de 016 no incluya nombres de empresas — leer el crudo local, no el md público (que ya no los trae).
  - **Además, avance de Pitch & Video (en la misma rama):** guion del video de 2 min cronometrado + tramo de implementabilidad con diagrama (mermaid validado) + las 5 preguntas a mentores listas para enviar → `docs/pitch/` (tickets [015](../tasks/015-guion-y-video.md) y [020](../tasks/020-tramo-implementabilidad.md), en borrador; se graba el sábado tras la integración 014). **Pendiente de Mani: enviar las preguntas a mentores hoy y mergear `rol4-cierre` a `main`.**
- 2026-07-24 13:00 (hora Colombia) — **🆕 Tablero del especialista en `/asesor/tablero`** (rama `feat/tablero-especialista`, commits `95fb76e` + `4f19058`). `/asesor` responde "a quién llamo ahora"; el tablero responde **cuánto entra, cómo se reparte por afiliación y quién es el mejor de cada lado**. La cola quedó **intacta** (es el clímax del demo y tiene sus tests encima); solo se le agregó el enlace cruzado.
  - **Desplegado aparte, sin tocar el deploy del equipo:** https://shadcn-dashboard-bv.vercel.app/asesor/tablero (proyecto `prj_co7RtUFgLGuIrjx6zwnVEXCvt3RA`, cuenta de Alejandro). Env vars de Supabase cargadas ahí, así que **responde `origen: supabase`** — a diferencia de `mvp-reto-vivienda.vercel.app`, que sigue en fixtures. Las 4 rutas en 200.
  - **Puntaje 0-100 nuevo** ([`lib/scoring/puntaje.ts`](../../lib/scoring/puntaje.ts)): TS puro, pesos explícitos que suman 100, **con el desglose punto a punto obligatorio al lado** ([`TablaPuntaje.tsx`](../../app/asesor/_components/TablaPuntaje.tsx) en la ficha). `DESIGN.md` rechaza "el score reducido a un número grande con barra de progreso" — con la aritmética impresa deja de serlo. **El puntaje NO decide nada:** quien decide sigue siendo `Score.salida`; el puntaje solo ordena dentro de un grupo. `afiliadoEfectivo()` pasó a exportarse para que agrupar y puntuar no puedan contradecirse.
  - **Abierto por construcción** (era el pedido): la página itera **registries**. Agregar una cifra = un objeto en [`lib/tablero/metricas.ts`](../../lib/tablero/metricas.ts). Cambiar el eje de agrupación = una línea (`AGRUPADOR_ACTIVO` en [`agrupadores.ts`](../../lib/tablero/agrupadores.ts); `POR_ESTADO` y `POR_FUENTE` ya están escritos y probados sin usar). El test de invariantes recorre **todos** los agrupadores registrados, así que el próximo que alguien agregue queda cubierto sin escribir test.
  - **Telón sintético** ([`lib/fixtures/cola-historica.ts`](../../lib/fixtures/cola-historica.ts)): 57 leads deterministas desde las 303 identidades ya versionadas, **pasados por el motor y el matcher reales**. Existen porque "leads por día" con 3 leads del mismo día no es una serie. Van marcados `sintetico: true` y **la pantalla lo avisa** — los 3 del demo siguen siendo los reales.
  - **🔎 Hallazgo, sirve para el pitch:** los **18 proyectos ya venden por encima del 10%** que permite la regla 90/10, así que el matcher no le deja **ningún** proyecto a un no afiliado que sí pasa el corte financiero. Esos leads entran al tablero igual, con 0 proyectos y la explicación diciendo que **el bloqueo es de cupo, no del lead**. Es el 27,1% del histórico apareciendo solo en la operación.
  - **⚠️ Trampa nueva, ya arreglada:** `lib/scoring` importaba con extensión `.js` (`./config.js`) y **Turbopack no la resuelve**. Nunca se notó porque solo lo consumían los tests; apenas entró a un Server Component **tumbó las 4 rutas con 500**. Alineado con la convención de `lib/matching` (sin extensión). Si alguien vuelve a escribir `from "./algo.js"` en `lib/`, rompe el dev server.
  - **135 tests verdes** (91 antes → 44 nuevos), typecheck y lint limpios.
- 2026-07-24 11:30 (hora Colombia) — **🟢 IA en producción DIAGNOSTICADA y BLINDADA (Nico, Track A).** El 500 de `/api/chat` era **cold start**, ya no hipótesis. Medido en prod, 4 llamadas seguidas: `200·1.3s`, `200·2.0s`, `200·1.1s`, `500·7.5s`. **Lambda caliente responde en 1-2s (cumple el <2s del [ADR 0002](../adr/0002-stack-mvp.md)); lambda frío tarda ~7s y a veces tira 500**, por el intercambio JWT→OAuth de la cuenta de servicio de Vertex, que en un lambda frío no está cacheado. Esto confirma exactamente la sospecha de la entrada 10:55. **El fix de modelo-por-backend (`19f116b`) SÍ está desplegado y sirviendo** — la teoría del "build viejo" quedó descartada; `/api/leads` sigue en `origen: supabase`.
  - **Blindaje puesto:** timeout de **3s con `AbortController`** en [`components/chat/ChatWhatsApp.tsx`](../../components/chat/ChatWhatsApp.tsx) (función `agregarBot`). Si el primer token no llega en 3s (frío), aborta el fetch → cae al texto determinístico en vez de congelar "escribiendo…" los ~7s completos. En cuanto llega el primer chunk, `clearTimeout` y el stream fluye hasta el final. Los 3s separan limpio el caso caliente (1-2s medidos) del frío (500 a ~7s); cortar de más solo cuesta el *pulido* del LLM en ese mensaje, no la corrección (el fallback es la pregunta real). **typecheck + lint + 91 tests verdes.**
  - **Decisión de costo — CERRADA por el equipo: se queda en Vertex** (gasta el crédito de $300, no la tarjeta). Razón: el demo es **video**, el lambda estará caliente, y se verá el pulido del LLM en 1-2s. El AI Studio key (que evita el cold-start porque no hace intercambio OAuth) quedó descartado por costo; con el blindaje puesto, Vertex sobra para el video.
  - **🎬 Truco de grabación (crítico para el video):** Vercel enfría el lambda tras unos minutos sin tráfico. **Justo antes de dar REC hay que mandar un mensaje de calentamiento** (abrir el chat, escribir algo) para que el lambda esté caliente en la toma. Grabar de corrido; si se corta varios minutos, recalentar.
  - **Lo que queda (NO es de Track A):** `/api/explicacion` (Rol Calidad IA / ex-C, Manuel) tiene el **mismo** riesgo de cold-start y su consumidor necesita el mismo blindaje de timeout. Opcional y sin hacer: hacer visible el error del server en vez del 500 mudo de `controller.error()`.
- 2026-07-24 11:30 (hora Colombia) — ⚠️ **SUPERSEDIDA por la entrada de las 18:50** ([ADR 0004](../adr/0004-design-system-colsubsidio.md)): "el formato sellado" ya no es el sistema visual del repo. Se conserva como registro de por qué existió. **Identidad visual Colsubsidio aplicada a las 3 superficies.** Se instaló **impeccable** (`npx impeccable install` → `.claude/skills/impeccable/`, con su hook de detección de diseño) y se montó el sistema visual sobre la **guía de identidad cromática oficial**: amarillo `#ffd000`, azul `#0067b1`, grafito `#575756`.
  - **Dos documentos nuevos en la raíz, ambos canon:** [`PRODUCT.md`](../../PRODUCT.md) (verdad de producto: usuarios, propósito, restricciones, la paleta como compromiso de marca) y [`DESIGN.md`](../../DESIGN.md) (el sistema visual, con tokens en frontmatter + reglas nombradas). Quien toque UI lee `DESIGN.md` antes.
  - **La dirección: "el formato sellado".** La ficha de un lead es un formato oficial —reglado, foliado, sellado— que enseña todos sus renglones y cita la norma. De ahí salen las invariantes: **cero sombras** fuera del chat (la profundidad es reglado de 2px + capa tonal), esquinas casi rectas (3/6px), toda cifra medida en Geist Mono tabular (`.cifra`), el azul **tiñe regiones completas** (riel, banda de veredicto, sello) en vez de salpicar como acento, y el amarillo hace solo dos cosas: fondo de sello y **trazo de resaltador** — uno por pantalla, sobre el hecho que decide.
  - **Tokens en [`app/globals.css`](../../app/globals.css)**, con tema claro y oscuro completos. Ya no hay `zinc-*`/`gray-*`/`sky-*` sueltos en la app: todo pasa por token semántico (`campo`, `papel`, `tinta`, `borde`, `salida-suave`…), así que un cambio de paleta es un archivo.
  - **Decisión de producto: el chat conserva el verde real de WhatsApp** (`#075e54`, y su tema oscuro auténtico). El reconocimiento del canal vale más que la consistencia de marca en esa superficie; Colsubsidio aparece ahí en 3 puntos y ningún otro (avatar, nombre del contacto, banda amarilla del disclaimer).
  - **3 bugs de contraste encontrados y corregidos**, los tres invisibles en modo claro: el sello de nutrición en oscuro daba **1,56:1** (usaba `azul-profundo` de relleno, que se aclara en oscuro para poder ser texto); el hover del botón primario en oscuro, **1,88:1**, por el mismo motivo — ahora hay token propio `--campo-hover`; y el folio del riel quedaba en 4,45:1. De ahí salió **la Regla del Rol Único** en `DESIGN.md`: un color que cambia de valor entre temas sirve a un solo rol. **Los 22 pares de texto/fondo del sistema están medidos y pasan AA (≥4,5:1) en ambos temas.**
  - **Verificado:** `npx tsc --noEmit` limpio, `npm run lint` limpio, **91/91 tests verdes** (no se tocó ni una aserción), y las 3 rutas responden 200. Se añadió `.claude/**` a los ignores de ESLint: el herramental vendorizado de impeccable metía 138 warnings que tapaban el lint real de la app.
  - **Render revisado y aprobado** (11:46, Alejandro, en navegador). Durante la sesión la verificación fue por CSS servido y contraste calculado porque la extensión de Chrome no conectaba; el ojo humano llegó después y confirmó.
  - **⚠️ Trampa de operación que costó 20 min:** correr `npm run build` con `npm run dev` encendido **rompe el dev server** — ambos escriben en `.next`, el proceso queda colgado reteniendo el puerto 3000 sin responder, y da pantalla en blanco. El síntoma engaña: parece que la app se rompió. Se arregla con `taskkill /PID <pid> /F` y relanzar. **No correr build y dev a la vez.**
- 2026-07-24 11:11 (hora Colombia) — **El grilling de scope quedó integrado a `main` y los 4 roles de cierre están versionados** ([`roles-recta-final.md`](roles-recta-final.md), referenciado en `AGENTS.md`). Rebase limpio sobre el trabajo de Nico/Alejandro (IA caída + buyer personas): ambas entradas coexisten, nada se perdió. **🔴 Segundo hallazgo de privacidad al integrar:** `data/buyer-personas-vivienda.md` (que subió Alejandro, `d07dbe3`) es una transcripción del PPT `Buyer_Person.pptx` en **este repo público**, con **22 tablas de "Top empresas" con nombres reales** de Colsubsidio — rompe la misma restricción no-negociable que el leak de `plan-research`. Los agregados % del md sí sirven y alimentan el [ticket 016](../tasks/016-distribuciones-por-proyecto.md); los nombres propios salen. Nuevo [ticket 022](../tasks/022-sanear-buyer-personas-md.md) (dueño Rol 4), y el `.gitignore` hay que blindarlo (bloquea `*.pptx` pero no un `.md`).
- 2026-07-24 10:55 (hora Colombia) — **🔴 LA IA ESTÁ CAÍDA EN PRODUCCIÓN: `/api/chat` y `/api/explicacion` devuelven 500. Dueño para continuar: Nico.** El demo NO se rompe (el fallback determinístico funciona), pero cada mensaje del bot se cuelga ~6s antes de rendirse. Commits de esta tanda: `98ce3d0`, `8a68700`, `19f116b` (de Nico), `e254a80`. Todo en `main`.
  - **Se arreglaron 3 bugs reales por el camino, y ninguno era el que parecía.**
    1. **El modelo estaba retirado.** `gemini-2.5-flash` da 404 en AI Studio ("no longer available to new users"). La llamada al LLM **nunca se había ejecutado** desde que se cambió a Gemini, así que nadie lo sabía: sin credencial daba 503 y el fallback tapaba todo.
    2. **El modelo depende del backend.** Nico (`19f116b`) y yo llegamos al mismo diagnóstico en paralelo; se conservó su implementación. **Trampa: `gemini-2.5-flash` está RETIRADO en AI Studio pero VIVO en Vertex**, y los `gemini-3.x` no existen en Vertex. Una sola constante rompe una de las dos rutas siempre. Medido contra las dos APIs reales — la tabla está en el comentario de [`lib/gemini.ts`](../../lib/gemini.ts).
    3. **El JSON de la cuenta de servicio no parseaba** (`8a68700`). Pegar ese archivo en un panel web falla por comillas envolventes, base64, o saltos de línea crudos en el `private_key`. Ahora se toleran las tres.
  - **Lo que SÍ está verificado (medido, no supuesto):** con la cuenta de servicio del equipo, **Vertex funciona**: su JSON parsea, autentica (la cuenta tiene el rol) y la Vertex AI API está habilitada. **En local, con `GEMINI_API_KEY` desactivada para forzar la ruta Vertex:** `/api/chat` 200 en **0,82–1,33s** de primer token, `/api/explicacion` 200 en **0,73s**, citando los 6 factores y el Decreto 583 sin inventar cifras. Modelos en Vertex `us-central1`: `gemini-2.5-flash` 0,74s ✅ · `gemini-2.5-flash-lite` 0,38s · `gemini-2.5-pro` 2,38s y rechaza `thinkingBudget:0` ❌ · `gemini-2.0-*`, `gemini-3-*`, `gemini-flash-latest` no existen.
  - **🔴 EL PROBLEMA ABIERTO, para Nico.** En **producción** los dos endpoints con IA dan **500 consistente, siempre a los ~5,5–6,2s**. Ya NO es la credencial (si no parseara daría 503, y da 500 → o sea parsea y se intenta llamar a Vertex). **Llegó a funcionar una vez: 200 con primer token de 8,01s.** O sea Vertex responde desde Vercel, pero lentísimo comparado con el local. Hipótesis **no confirmada**: el intercambio JWT→token OAuth que exige la cuenta de servicio, que en local se cachea y en un lambda frío no; el patrón de ~6s constantes huele a timeout. **No verificado: falta leer los logs de Vercel**, que es lo primero que hay que hacer (yo no tengo acceso).
  - **Ojo con el número:** aun cuando funcionó, **8,01s de primer token incumple el <2s del [ADR 0002](../adr/0002-stack-mvp.md)**. Arreglar el 500 no basta; hay que recuperar la latencia o el chat no es creíble en el video.
  - **Lo que propongo y NO hice** (queda para quien siga): (1) **timeout en el cliente** con `AbortController` a ~2,5s en [`components/chat/ChatWhatsApp.tsx`](../../components/chat/ChatWhatsApp.tsx) — hoy el fallback existe y es correcto (`resp.ok` falso → catch → texto determinístico, líneas 108-127), pero **espera los 6s completos** antes de rendirse; con el timeout el demo deja de depender de Vertex pase lo que pase; (2) **hacer visible el error del servidor** — hoy `controller.error()` produce un 500 mudo y obliga a inferir.
  - **Decisión de costo, del equipo, sin resolver:** Vertex gasta el crédito de $300 de Google Cloud pero hoy está en 500 y midió 8s; `GEMINI_API_KEY` de AI Studio está **verificada funcionando a 1,68s** pero cobra a la tarjeta. Con el timeout del cliente puesto, el demo sobrevive con cualquiera de las dos.
  - **Estado del repo:** 91 tests, typecheck, lint y build verdes. 12 tests nuevos en [`lib/gemini.test.ts`](../../lib/gemini.test.ts) que fijan la correspondencia backend→modelo (y exigen que los dos modelos sigan siendo distintos), las 4 formas de pegado de la credencial, y que el mensaje del 503 **nunca** filtre el valor de una credencial. Las páginas y `/api/leads` siguen en 200: lo único caído es la IA.
- 2026-07-24 10:52 (hora Colombia) — **Grilling de scope (Mani): 10 decisiones, 9 vacíos, recast a 4 roles de cierre.** No tocó código; fijó el rumbo a ~48h del deadline. **Decisiones (no re-litigar):** subsidios = tabla simple fundamentada de Colsubsidio (el motor resta el subsidio antes del corte del 40%, ticket [017](../tasks/017-tabla-subsidios.md)); trigger de nutrición = **híbrido** (fecha solo si la regla fallida es temporal y derivable del dato del lead, condición pura si no; el personaje de nutrición del demo es el caso CON fecha); similitud = **distribución por proyecto** (hoy `distribuciones.json` es global → no calculable; tickets [016](../tasks/016-distribuciones-por-proyecto.md) y [018](../tasks/018-similitud-en-explicacion.md)); buyer personas del PPT **entran** al workflow (016); griegos = clusters anónimos `[inferido]` (ya como B lo hizo); panel de impacto = **franja** de 3 cifras en `/asesor`, timeboxed ([019](../tasks/019-franja-impacto.md)); cédula se sostiene + pregunta a mentor; video = screen recording real + tramo de implementabilidad ([020](../tasks/020-tramo-implementabilidad.md)). **Vacíos hallados:** el PPT no alimentaba nada; `distribuciones.json` era global, no por proyecto; el video (015) sin dueño; la cadena aún no conecta (el chat hace `console.log`, el orquestador 006 pendiente); producción sigue en fixtures (env vars de Supabase sin cargar en Vercel); Gemini sin smoke test real. **Recast a 4 roles de cierre** (Integrador ex-A · Datos&Motor ex-B · Calidad IA&Demo ex-C · Pitch&Video ex-D), cada quien conserva su código; los prompts por rol quedaron en el plan aprobado de la sesión. Tickets nuevos [016](../tasks/016-distribuciones-por-proyecto.md)–[021](../tasks/021-plan-research-privado.md). **🔴 Sigue el leak de `plan-research` público** (ver bullet 09:15): Rol 4 lo pone privado primero que nada ([021](../tasks/021-plan-research-privado.md)).
- 2026-07-24 11:45 (hora Colombia) — **Rol 2 (Datos & Motor): motor de scoring rediseñado a 2 capas — puntaje ponderado 0–100.** Antes era 1 sola regla dura (40%) → 3 buckets binarios, sin forma de priorizar la cola. Ahora: **Capa 1** = gate legal 40% (Decreto 583, sin cambios, bloquea → nutrición); **Capa 2** = `puntaje` 0–100 compuesto ponderado de 6 factores (pesos en `lib/scoring/config.ts::PESOS`, propuestos/ratificables). Cada `FactorScore` expone `peso`/`valor_norm`/`aporte`; `puntaje = Σ aportes` → 100% trazable, sigue siendo cero-caja-negra. Personajes rankean María 63 > Carlos 41 > Laura 0. 97 tests verdes (13 en el motor, +6 nuevos: determinismo, monotonía en holgura, additividad, gate sigue bloqueando), tsc + lint limpios.
  - **⚠️ CAMBIO DE CONTRATO en `lib/types.ts` (additivo, no rompe a nadie) — para Rol 1 (ticket 002) y Rol 3/D:** `Score` gana `puntaje: number` (requerido); `FactorScore` gana `peso?`/`valor_norm?`/`aporte?` (opcionales). Ripple ya resuelto por mí: fixtures de `lib/fixtures/scores.ts` (+`puntaje`), round-trip de `lib/leads-repo.ts` (`FilaLead.puntaje` + ambos mapeos), y columna `puntaje integer` en `db/schema.sql`. **Rol 1:** al mergear ticket 002, dejar `puntaje` como está. **Rol D:** la vista `cola_asesor` puede ordenar por `puntaje DESC` para priorizar la cola de verdad (hoy todo "listo" es plano). **Rol C:** el matcher/explicación puede citar los factores de mayor `aporte`.
  - **Coherencia data sintética vs. PPT (`data/buyer-personas-vivienda.md`):** misma tesis, mismo ranking por proyecto, pero el PPT corre ~11 pts más alto en no-afiliación (Total 38% vs. nuestro 27,1%; distinto corte, 15/01/2026). El PPT tiene defectos propios (Género no confiable, etiquetas salariales cruzadas) → cross-check, no fuente. Pitch: mantener 27,1%, opcional citar el 38% de la propia caracterización de Colsubsidio. Ambos >> 10%.
  - **Pendiente Rol 2 (plan aprobado):** similitud real por proyecto (016, hoy `valor_norm=0.5` provisional), tabla de subsidios (017), trigger híbrido, enriquecimiento cédula (003), regla 40% compartida (004), franja de impacto (019).
- 2026-07-24 09:15 (hora Colombia) — **Track B: `data/sintetica/proyectos.json` reescrito a la forma exacta que espera el matcher de C** (`lib/matching/tipos.ts::FichaProyecto`), rama `Track-B` (`0ca2fe5`, `05bbfcc`). Cambios: `precio_tipico`→`precio_desde` (pesos ya ÷10.000), `ubicacion` partido en `ciudad`/`zona`, el booleano de cupo reemplazado por `cupo_no_afiliados: {usado, total}` (`usado` = conteo real histórico de no afiliados, `total` = 10% del volumen histórico — proxy de inventario, el Excel no trae unidades totales), `link_brochure`/`link_360` → `brochure`/`recorrido_360`, y `vis` nuevo (heurística: precio ≤ ~150 SMMLV). `lib/types.ts::ProyectoCatalogo` y `lib/scoring/index.ts` actualizados para consumir la nueva forma. **Los 3 personajes canónicos del demo (`lib/fixtures/leads-evento.ts`) siguen con `proyecto_interes` inventado** (Torres de Bellavista, Reserva del Poblado, Alameda del Río) — no son parte del catálogo real de 18 proyectos; sigue abierto en el ticket [001](../tasks/001-personajes-canonicos.md), dueño A. `npm test` (motor de scoring) y `npx tsc --noEmit` en verde tras el merge con `main`.
  - **⚠️ Hallazgo de seguridad, sin resolver:** el repo hermano `plan-research` es **público en GitHub** y tiene commiteados los 3 archivos de data real de Colsubsidio (`hackathon_VIVIENDAv2.xlsx`, `Links brochures .xlsx`, `Buyer Person.pptx`, commit `8bc42eb3`) — descargables sin login. Rompe la restricción no-negociable de `AGENTS.md`. Pendiente: alguien con permisos pone el repo en privado o se reescribe el historial para sacar esos 3 archivos.
- 2026-07-24 00:49 (hora Colombia) — **Track D MERGEADO a `main` (`db137f7`) y Supabase VIVO.** El link público del demo ya sirve `/asesor`: https://mvp-reto-vivienda.vercel.app/asesor
  - **La DB existe y está poblada.** Proyecto de Supabase creado, `db/schema.sql` + `db/seed.sql` corridos, los 3 personajes con sus 35 mensajes de conversación. **En local `/api/leads` ya responde `origen: supabase`.**
  - **Los CHECK constraints se probaron contra el Postgres real**, no solo parseando SQL. Los 6 disparan: lead calificado sin factores (criterio 2), nutrición sin regla ni trigger (criterio 3), listo con 1 solo proyecto (criterio 4), estado `descartado`, consentimiento sin timestamp y fuente fuera del contrato. **RLS confirmado:** sin la clave secreta, la API devuelve 401 y no lee nada.
  - **El re-enganche persiste de verdad:** `re_enganchado_en` queda en Postgres y el evento deja su fila `rol='sistema'` con el motivo exacto — de ahí lo lee A. Probado y luego **devuelto a estado limpio** para que el jurado lo encuentre fresco.
  - **⚠️ PRODUCCIÓN SIGUE EN FIXTURES.** Falta cargar `NEXT_PUBLIC_SUPABASE_URL` y `SUPABASE_KEY` en **Vercel → Settings → Environment Variables** y **redesplegar** (las env vars no aplican a un build ya hecho). Hasta entonces el demo funciona pero no persiste, y lo avisa en pantalla.
  - **Riesgo de demo cazado:** `globals.css` pone el body casi negro con `prefers-color-scheme: dark` y toda la vista del asesor usa colores claros — un jurado con el sistema en oscuro habría visto los títulos invisibles. Arreglado **solo en las páginas de D**, sin tocar el archivo compartido de A.
  - 72 tests verdes (4 de A + 11 de C + 57 de D), typecheck, lint y build limpios. El `vitest.config.ts` que introduje no rompió los suites existentes.
- 2026-07-23 (noche) — **Proveedor de IA cambiado de Claude a Google Gemini** (`gemini-2.5-flash`). A la hora de deployar solo había key de Gemini, no de Anthropic; como manda "que funcione el domingo", se cambió el proveedor. El proveedor quedó **aislado en un solo archivo, [`lib/gemini.ts`](../../lib/gemini.ts)** (streaming + thinking off), y los dos endpoints con IA lo consumen. **La env var pasó de `ANTHROPIC_API_KEY` a `GEMINI_API_KEY`** (`.env.example`, ADR 0002 con nota, AGENTS.md, todo actualizado); `@anthropic-ai/sdk` desinstalado. Los contratos duros no cambian: streaming obligatorio, key solo server-side, scoring sin LLM. **⚠️ Manuel (Track C): esto tocó tu `/api/explicacion`** — mismo comportamiento, solo cambié el cliente LLM; tu matcher, prompts y system prompt quedaron intactos. Si prefieres volver a Claude o cambiar de modelo, es un cambio de una línea en `lib/gemini.ts`. **Sin verificar en vivo:** la llamada real a Gemini no se probó (no había key en la sesión); sí se verificó `npm run build` en verde, los 72 tests, y que sin key ambos endpoints responden 503 → fallback determinístico. Al agregar `GEMINI_API_KEY` (local o en Vercel) hay que hacer el smoke test real de los 3 personajes.

- 2026-07-23 22:50 (hora Colombia) — **Track D construido contra fixtures.** [ADR 0003](../adr/0003-esquema-db-leads.md) + SQL + `/asesor` + mis mitades de los tickets 005, 007 y 012.
  - **[ADR 0003](../adr/0003-esquema-db-leads.md) cierra el modelo del [plan §2](../plan.md).** Tres tablas, campos sacados de `lib/types.ts` sin inventar. Me aparté del insumo en tres puntos, con la razón en el ADR: `lead_id` es **text, no uuid** (manda el contrato); `conversaciones` agrega el rol **`sistema`** a los del plan (los eventos no son mensajes de nadie, y el re-enganche tiene que quedar auditable); y **`citas` guarda solo la franja elegida** — el catálogo es [`data/sintetica/slots.json`](../../data/sintetica/slots.json), porque un horario simulado no muta y el ADR 0002 dice que a la DB va solo lo que muta. El SQL está en [`db/schema.sql`](../../db/schema.sql) + [`db/seed.sql`](../../db/seed.sql), validado contra la gramática real de Postgres. **Falta crear el proyecto de Supabase y compartir credenciales** — hasta entonces todo corre con fixtures y la UI lo avisa en pantalla.
  - **Los criterios de aceptación se enforcean como CHECK constraints:** si un track manda un lead de nutrición sin trigger, o un lead calificado sin factores, el insert falla en desarrollo y no en el demo. **A ratificar en el kickoff**, junto con que el re-enganche **no sea un cuarto estado** (va como `re_enganchado_en`, para no tocar `Score.salida`).
  - **Descarté las fixtures que había inventado y ahora todo consume `lib/fixtures/`** (ticket 001). Era exactamente el riesgo de la costura S6. `db/seed.sql` quedó como **espejo** de las fixtures, no como fuente, y lo dice en su encabezado.
  - **Ticket 005 (agendador), mitad de D lista:** `slots.json` + `GET /api/citas` (catálogo, no toca la DB) + `POST /api/citas` (persiste la elegida; 422 si la franja no está en el catálogo) + la cita en la ficha. **Bug encontrado corriendo la app:** `slots.json` arrancó con ids inventados mientras las fixtures usan `p-07`/`p-03`; el chat habría pedido franjas y recibido una lista vacía sin que nada fallara. Corregido y con test que lo impide.
  - **Ticket 007 (re-enganche), mitad de D lista:** el botón lleva al chat en **un clic** (`/?lead_id=X&reenganche=1`) y deja el trigger exacto en `conversaciones` con `rol='sistema'`, para que A no tenga que inventarse el motivo. **⚠️ El criterio 3 NO está verificado:** `app/page.tsx` hoy guarda todo en estado de React y **no lee la URL**, así que el clic aterriza en el landing. Detalle y contrato en el [ticket 007](../tasks/007-reenganche-nutricion.md).
  - **Ticket 012 (test del criterio 2): con dientes.** El `.map()` solo no alcanzaba — un factor nuevo aparecería, pero con su nombre técnico crudo delante del jurado. El test compara los nombres del motor contra `ETIQUETA_FACTOR` y falla con instrucciones. Verificado inyectando un factor en las fixtures: falla, y vuelve a verde al revertir. **B:** cuando el motor real reemplace las fixtures, correr `npm test`.
  - **La fuente del lead (meta/google/web) ahora se ve en la ficha.** Se registraba desde la ingesta y no aparecía en ninguna pantalla; es lo que sostiene la narrativa multi-canal del [spec §4 paso 1](../spec.md).
  - **Contrato para los demás:** nadie escribe a Supabase directo. `POST /api/leads` recibe un `LeadCurado` completo; `POST /api/citas` persiste la franja. 61 tests verdes, typecheck, lint y build limpios. `@vitejs/plugin-react` fijado en `^5` para no obligar a subir de vitest 3.
- 2026-07-23 17:30 (hora Colombia) — **Track C: las 3 fases del arranque, hechas, en `feature/matching`** (commits `57615c9`, `c6acca3`, `5f05201`). Rama consolidada: `mani-TrackC` se borró (no tenía commits propios) y `feature/matching` se puso al día con `main` por fast-forward, así que la convención del reparto vuelve a mandar. Lo hecho:
  - **Fase 1 — [`docs/explicaciones-referencia.md`](../explicaciones-referencia.md)**: las 3 explicaciones "perfectas" escritas a mano sobre los personajes canónicos de A (`lib/fixtures/`), no sobre números inventados. Trae la **convención de las dos voces** (`LeadCurado.explicacion` en tercera persona para el asesor · `ProyectoRecomendado.porque` en segunda para el lead), la **checklist de cobertura de los 6 factores** (así el criterio de aceptación 2 se verifica contando, no a ojo) y 6 criterios ordenados para evaluar el output del modelo. Doble uso: vara de medir del prompt **y fallback del [ticket 010](../tasks/010-fallback-conversador.md)** si `/api/explicacion` no responde en pleno demo.
  - **Fase 2 — `lib/matching/`**: matcher determinista (TS puro, sin LLM ni red) que elige 2-3 proyectos por reglas explícitas y deja la **traza** de por qué entró cada uno, que es lo que después cita el experto. Descarta por precio contra `precio_maximo` y por cupo 90/10 si el lead es no afiliado; no saca al lead de su zona si hay 2+ proyectos ahí. **11 tests** verdes: un caso por personaje + el lead que llega sin nada (sin perfil, sin zona, sin proyecto de interés).
  - **Fase 3 — `lib/matching/prompt-experto.ts` + `/api/match` + `/api/explicacion`**: el experto solo puede usar los datos del mensaje (prohibido inventar montos, subsidios o características), cita los 6 factores y el Decreto 583 textual. `/api/match` es determinista y responde JSON de una vez; `/api/explicacion` va en **streaming** con `claude-opus-4-8` y la key solo server-side. Verificado contra el dev server: los 3 personajes por `/api/match` dan lo esperado (Diana 3 proyectos en Bogotá, Carlos 2 en Medellín **ambos con cupo**, Yuliana cero).
  - **Dos decisiones que el equipo debe saber:** (1) `FichaProyecto` (la forma que el matcher espera de `data/sintetica/proyectos.json`) está definida en `lib/matching/tipos.ts` y **hay que anunciársela a B antes de que publique el JSON esta noche** — si no coincide, el matcher se reescribe el viernes; (2) `/api/explicacion` va **sin thinking** a propósito: es redacción sobre hechos ya calculados y lo que se juega es el primer token (< 2s), igual que `/api/chat` de A. Si el equipo prefiere `adaptive thinking` como dice el [ADR 0002](../adr/0002-stack-mvp.md), se cambia en una línea.
  - **C no reimplementó el tope del 40%** ([ticket 004](../tasks/004-capacidad-compartida.md)): el matcher consume un `precio_maximo` numérico. Mientras `Score` no lo traiga ([ticket 002](../tasks/002-contratos-capacidad-en-score.md)) viaja como parámetro y cae a una fixture provisional por personaje. **Pendiente de C:** reemplazar `lib/matching/fixtures.ts` por el catálogo real de B, y evaluar el output del experto contra las explicaciones de referencia (necesita `.env` con `ANTHROPIC_API_KEY`, que hoy no existe local).
- 2026-07-23 17:05 (hora Colombia) — **[`docs/plan.md`](../plan.md) + 15 tickets en [`docs/tasks/`](../tasks/README.md)** (`/plan`). El plan no repite el reparto: cubre lo que el reparto **no** asignó. Contenido: la app en un mermaid, el modelo de datos propuesto (insumo del ADR 0003 de D, no lo reemplaza), **9 costuras entre tracks sin dueño**, la secuencia hasta el freeze y la trazabilidad criterio → pieza. Hallazgos duros: (1) el **orquestador** de la cadena no era de nadie y se iba a descubrir el sábado — ahora es el ticket 006 y va el **viernes**; (2) los **3 personajes del demo** los estaban inventando los 4 tracks por separado (ticket 001, hoy, en el scaffold); (3) el **enriquecimiento real** (cédula → `PerfilConocido`) no existía, y sin él el criterio 1 no se puede verificar (ticket 003); (4) la **regla del 40%** iba a quedar implementada dos veces, en B y en C (ticket 004); (5) el **agendador** decía "lo hace D o A" = nadie, y el criterio 4 exige cita (ticket 005); (6) **nadie tenía el video**, que es el entregable con pre-filtro (ticket 015, **sigue sin dueño**). Dos cambios propuestos a `lib/types.ts` ([plan §8](../plan.md)) a ratificar en el kickoff: `Score.precio_maximo` y `/api/match` recibiendo `{ lead, score }`. Los 4 prompts de arranque ya listan los tickets de su track. **Higiene:** `docs/plan-hackathon.md` → `docs/agenda-evento.md` (un solo "plan" en el repo) y `handoff.md` devuelto a `docs/agents/` (estaba en `docs/` con todos sus links relativos rotos).
- 2026-07-23 15:52 (hora Colombia) — **Distribución de roles por track documentada** — resumen rápido en [`docs/prompts/README.md`](../prompts/README.md), contratos y tareas completas en [`docs/reparto-inicial.md`](../reparto-inicial.md). Cada track tiene su prompt de arranque en `docs/prompts/prompt-{a,b,c,d}.md`:
  - **Track A — Conversación & entrada:** scaffold de Next.js (desbloquea a todo el equipo) + chat estilo WhatsApp — landing con los 3 personajes del demo, conversación adaptativa que pregunta solo lo que falta, conectada a Claude en streaming. Output: `Lead`.
  - **Track B — Scoring & datos:** limpieza del Excel real → `data/sintetica/` (identidades, proyectos, distribuciones — punto de sincronización del equipo) + motor de scoring en TS puro (reglas, sin LLM): afiliación 90/10, tope de cuota ≤40%, subsidio, similitud. Output: `Score`.
  - **Track C — Matching & explicación:** dado el `Score` de B, recomienda 2-3 proyectos por reglas y redacta el porqué en lenguaje natural con el "experto en vivienda" (LLM grounded, nunca inventa datos). Output: `LeadCurado` sin cita.
  - **Track D — Vista asesor, DB & nutrición:** esquema de Supabase (leads/conversaciones/citas, ADR 0003) + `/asesor` — cola priorizada + ficha del lead (score desglosado, explicación, proyectos, cita), el clímax del demo. Incluye el botón "simular trigger" de nutrición.

  Todos consumen `lib/types.ts` por fixture, no el código de los demás — se integra el sábado.
- 2026-07-23 16:31–16:49 (hora Colombia) — **Track A: scaffold + Fase 2 completos, mergeados a `main` desde la rama `Nico`**. Fase 1 (scaffold): `create-next-app` (TS, App Router, Tailwind) sin pisar `AGENTS.md`/`CLAUDE.md`/`docs/`/`.gitignore`; `lib/types.ts` con los 7 contratos exactos de [`docs/reparto-inicial.md`](../reparto-inicial.md); `lib/fixtures/` con los 3 personajes del spec §4 (afiliado listo, no afiliado listo, nutrición) cubriendo `LeadEvento`→`LeadCurado`; `.env.example` sin valores; los 3 feedback loops de `AGENTS.md` ya corren tal cual están documentados (no hubo que tocar `AGENTS.md`). Fase 2 (Track A): landing del jurado en `/` (3 tarjetas + "soy yo"), chat estilo WhatsApp (`components/chat/`), lógica adaptativa en `lib/conversacion/preguntas.ts` (nunca repregunta un dato que `PerfilConocido` ya trajo, criterio de aceptación 1 verificado con Playwright), y `/api/chat` conectado a `claude-opus-4-8` con streaming — con fallback automático al texto determinístico si no hay `ANTHROPIC_API_KEY` o falla la red, para que el flujo nunca dependa de la IA. **Sin conectar todavía:** el output de la conversación (`Lead`) solo se hace `console.log`, no hay POST a ningún `/api/leads` (eso es Track D). **Pendiente:** conectar Vercel (dashboard, manual) y avisar "scaffold listo" al grupo.
- 2026-07-23 — **STACK DECIDIDO: Next.js + Vercel + Supabase + API de Claude** — [ADR 0002](../adr/0002-stack-mvp.md). Reglas duras: monolito Next.js con API routes (un solo deploy, auto-deploy al pushear a `main`); datos estáticos como JSON en `data/sintetica/` generados por Python **offline** en `scripts/` (Python nunca en producción); Supabase solo para lo que muta (leads/conversaciones/citas); IA solo en `/api/chat` y `/api/explicacion` con `claude-opus-4-8` en **streaming obligatorio** (primer token < 2s); el scoring es TS puro sin LLM; API key solo server-side (el repo es público); contratos entre tracks (`Lead`/`Score`/`LeadCurado`) en `lib/types.ts`. Con esto quedaron llenados los 3 feedback loops y los TODOs de arquitectura/performance/escala en [`AGENTS.md`](../../AGENTS.md), y cerrados los supuestos "Stack" y "Performance" del [spec §7](../spec.md). **Siguiente:** scaffold de Next.js + `/plan`.
- 2026-07-23 — **[`docs/spec.md`](../spec.md) escrito** (entrevista `/spec`, 7 bloques). Cierra las abiertas del layout: **3 salidas del corte** (listo / listo-con-restricción-de-cupo / nutrición, con el 90/10 como marca), **entrada del demo** = 3 personajes pre-sembrados + "soy yo", **vista del asesor** = cola + detalle (panel de impacto opcional si sobra tiempo), **ingesta simulada** con lead-evento de esquema estándar, **cédula** como llave del enriquecimiento, **consentimiento habeas data** registrado en el primer mensaje, **agendador** con slots simulados, **triggers de nutrición** = inversa de la regla que falló. **4 criterios de aceptación** (uno por tramo del demo). Hallazgo duro: el **40% cuota/ingreso es normativa vigente**, no heurística ([Decreto 583 de 2025](https://minvivienda.gov.co/normativa/decreto-0583-2025), que subió el techo desde el 30% del Decreto 145 de 2000) — se cita textual en la explicación al asesor. Catálogo confirmado: **18 proyectos** (unión de las 2 hojas de `Links brochures .xlsx`; 16 con brochure, 17 con 360), y `VIBO ONCE`/`KARAKALI` traen ubicación contradictoria entre hojas.
- 2026-07-23 — **Contrato migrado a [`AGENTS.md`](../../AGENTS.md)** (re-corrida de `/scaffold` tras su refactor). `AGENTS.md` es ahora el contrato canónico **tool-neutral** — quien use Codex/Cursor/Gemini lee la misma fuente de verdad — y `CLAUDE.md` quedó como puntero delgado con `@AGENTS.md`. Nuevo ahí: la **constitución** de 4 restricciones no-negociables (cero caja negra · demo autogestionado · data de Colsubsidio nunca al repo público · deadline 26 jul 11:30 a.m.), la tabla de **Contratos** (fila datos/privacidad, enforced por `.gitignore`) y los 3 **feedback loops en `TODO`**. Añadido `.claude/settings.json` con permission floor (deny `rm -rf`, `push --force`, `reset --hard`, `clean -fd`); `settings.local.json` sigue ignorado. **Acción para quien elija el stack: llenar los 3 feedback loops de `AGENTS.md` de una vez.**
- 2026-07-23 — **Scaffold de este repo de código.** Estructura canónica montada: `CLAUDE.md`, `README.md`, `.gitignore`, `docs/agents/{context,handoff}.md`, `docs/adr/{0000,0001}`, `docs/reto/` (brief + doc de insumos), `docs/mvp-layout.md`, `docs/agenda-evento.md`, `docs/URGENTE-Y-NOTICIAS.md`. Los 6 archivos del brainstorm inicial se reorganizaron desde la raíz y se les arreglaron las rutas. **Data real** (`hackathon_VIVIENDAv2.xlsx`, `Buyer Person.pptx`, `Links brochures .xlsx`) movida a `docs/recursos-reto/` y **excluida de git** (repo público, data de Colsubsidio). Sin código de app todavía: el stack se decide en el kickoff. **Siguiente:** kickoff → `/spec`.
- 2026-07-23 — **Scope macro del MVP definido** (grill de Mani + Claude). 8 decisiones cerradas (demo = viaje completo con clímax en asesor, WhatsApp simulado + disclaimer, nutrición con triggers condicionales, scoring de reglas transparentes + similitud + LLM explica, conversación adaptativa, bot agenda visita, no-afiliado sigue el flujo hacia DB central, workflow orquestado con IA puntual) y las abiertas marcadas. Todo en [`docs/mvp-layout.md`](../mvp-layout.md) — incluye mermaid strawman a curar en kickoff.
- 2026-07-23 — **Datos reales del reto analizados** (viven en `docs/recursos-reto/` local y en `plan-research`). **Munición de impacto:** 27,1% de los compradores históricos NO son afiliados (vs. 10% de la regla 90/10); los 16 proyectos con ubicación conocida incumplen el límite (15,9%–63,1%). **Realidades del Excel:** no hay columna "afiliado" (se infiere de `PERIODO_AFILIADO`), `VLR_VIVIENDA` trae 4 ceros de más (÷10.000), y `SEGMENTO_POBLACIONAL`/`CATEGORIA`/`PIRAMIDE_NUEVA` vienen con letras griegas. Detalle en el dashboard del equipo (artifact en `plan-research`, no versionado). Canal medido (`MEDIO`): 43,4% de compras vienen de señalización física, solo 8,3% de canales digitales.
- 2026-07-23 — **RETO ELEGIDO: Vivienda** (perfilamiento inteligente de leads). [`ADR 0001`](../adr/0001-eleccion-reto-vivienda.md). No se re-litiga.
- Contexto de la hackathon, retos, reglas y criterios: ver [`CLAUDE.md`](../../CLAUDE.md) y el repo `plan-research`.

## Roadmap

### Now (ready — sin dependencias pendientes)

- [ ] **🔴 Arreglar el ingreso: hoy todo lead nuevo cae a nutrición.** El motor lee `ingreso_hogar_mensual` (número) y la conversación solo llena `rango_ingreso_hogar` (texto). Nadie los conecta, así que el gate del 40% falla por dato faltante y manda a nutrición a cualquiera que entre por "soy yo". Hay que elegir hoy: preguntar un monto, o convertir el rango a su punto medio. Mismo problema con `subsidio_monto_mensual` (el subsidio nunca baja la cuota) y con `situacion_crediticia` (texto libre donde el tipo espera un enum). Detalle en [spec 02](../specs/02-conversador.md).
- [ ] **Reunión del TEAM sobre el [paquete de specs](../specs/README.md).** Las decisiones que hay que ratificar o tumbar, en orden de urgencia:
  1. **Cuál escala de puntaje es la canónica** ([spec 03](../specs/03-scoring.md) D6) — hoy la pantalla muestra un número distinto al que calcula el motor.
  2. **¿El LLM conduce la conversación o sigue conduciendo el código?** ([spec 02](../specs/02-conversador.md) D1) — decisión de riesgo con el calendario encima.
  3. **Qué le decimos al no afiliado que califica y no tiene cupo** ([spec 04](../specs/04-match-agenda.md) D3) — la parte más delicada del demo y la más potente del pitch.
  4. **Los pesos del motor y el 0,6% de la cuota** ([spec 03](../specs/03-scoring.md) D2 y D4) — `spec.md §7` los tiene abiertos desde el principio.
  5. **¿Adoptamos "propenso / no propenso"?** ([spec 06](../specs/06-dashboard-asesor.md) D1) — son las palabras del mentor, pero chocan con "nadie se descarta".
  6. **¿Arreglamos los IDs de `slots.json`?** ([spec 04](../specs/04-match-agenda.md) D4) — sin eso la cita no funciona con proyectos reales.

- [x] ~~Revisar el render de la identidad visual en un navegador~~ — **hecho 2026-07-24 11:46, visto por Alejandro: se ve bien.** Si algo hay que ajustar después, el sistema está en [`DESIGN.md`](../../DESIGN.md) y los tokens en [`app/globals.css`](../../app/globals.css): se cambia el token, no el componente.
- [ ] **🔴 Arreglar la IA caída en producción — dueño: Nico.** `/api/chat` y `/api/explicacion` dan **500 consistente a los ~6s**. NO es la credencial (parsea: si no, daría 503). Detalle completo en la entrada del 2026-07-24 10:55 de Memory. Orden sugerido:
  1. **Leer los logs de Vercel** del deploy actual y buscar el error real del 500 — todo lo demás es inferencia hasta tener eso.
  2. **Poner el timeout en el cliente** (`AbortController` ~2,5s en [`ChatWhatsApp.tsx`](../../components/chat/ChatWhatsApp.tsx)). Es lo que blinda el demo pase lo que pase: hoy el fallback funciona pero espera los 6s completos.
  3. **Hacer visible el error del servidor** en vez del 500 mudo de `controller.error()`.
  4. **Recuperar la latencia**: aun cuando funcionó, el primer token fue **8,01s** (en local con las mismas credenciales: 0,82–1,33s). El [ADR 0002](../adr/0002-stack-mvp.md) exige < 2s.
  - **Decisión de costo pendiente del equipo:** Vertex (gasta el crédito de $300, hoy en 500) vs. `GEMINI_API_KEY` de AI Studio (verificada a 1,68s, cobra a la tarjeta).
- [ ] **🔴 Rol 4: poner `plan-research` en privado** ([ticket 021](../tasks/021-plan-research-privado.md)) — leak de data real de Colsubsidio. Primero que todo lo demás.
- [ ] **Recta final por 4 roles de cierre (grilling 2026-07-24).** Tickets nuevos [016](../tasks/016-distribuciones-por-proyecto.md)–[021](../tasks/021-plan-research-privado.md); el reparto A/B/C/D se reencuadra en los 4 roles (Integrador · Datos&Motor · Calidad IA&Demo · Pitch&Video). Detalle en Memory (2026-07-24 10:52) y en [`URGENTE-Y-NOTICIAS.md`](../URGENTE-Y-NOTICIAS.md).
- [ ] **Conectar Vercel (dashboard, manual) y avisar "scaffold listo" al grupo** — el scaffold + Track A ya están en `main` (rama `Nico` integrada y pusheada). Falta que Nico conecte el deploy en el dashboard de Vercel (preset Next.js, root `./`, env vars de `.env.example`); después de eso los otros 3 tracks tienen su código corriendo en producción, no solo local.
- [ ] **🔴 Cargar las env vars de Supabase en Vercel y redesplegar.** La DB ya existe y funciona en local, pero **producción sigue en modo fixture**: el `.env` es solo la máquina de quien lo creó. Vercel → Settings → Environment Variables → `NEXT_PUBLIC_SUPABASE_URL` y `SUPABASE_KEY` (la **secreta**, `sb_secret_...`) → **Redeploy** (las env vars no aplican a un build ya hecho). Verificar con `curl https://mvp-reto-vivienda.vercel.app/api/leads` → debe decir `origen: supabase`. Las credenciales se comparten por el canal del equipo, **nunca al repo**.
- [ ] **Track A: leer `?lead_id=` en `/` para cerrar el criterio 3** ([ticket 007](../tasks/007-reenganche-nutricion.md)). La mitad de D está lista y el botón ya lleva al chat, pero hoy `app/page.tsx` no lee la URL y el clic aterriza en el landing. El motivo del re-enganche no hay que inventarlo: viene en `trigger_nutricion` (`GET /api/leads/<id>`) y en la última fila `rol='sistema'` de la conversación.
- [ ] **Track A: ofrecer franjas en el chat** ([ticket 005](../tasks/005-agendador.md)). `GET /api/citas?proyecto_id=p-07&limite=3` ya devuelve el catálogo y `POST /api/citas` persiste la elegida.
- [x] ~~**B: recibir de C la forma de `FichaProyecto`**~~ — hecho 2026-07-24 09:15, ver Memory arriba. `data/sintetica/proyectos.json` ya sale en la forma exacta que espera `lib/matching/tipos.ts`. **Pendiente de C:** reemplazar `lib/matching/fixtures.ts` por este JSON real.
- [ ] **Kickoff con todo el equipo** — ahora arranca del [`spec.md`](../spec.md), no del layout. Ratificar la frase de apuesta y resolver los supuestos marcados "decidir en el kickoff": umbral del corte y peso de cada factor, reglas concretas de subsidio, trigger de nutrición con plazo estimado vs flow de revisión periódica, y si el panel de impacto entra. Es el Día 1 del [`docs/agenda-evento.md`](../agenda-evento.md).
- [ ] **Arrancar los 4 tracks en paralelo** según [`docs/reparto-inicial.md`](../reparto-inicial.md): A (conversación + scaffold, paso 0), B (limpieza Excel → `data/sintetica/` + motor de scoring), C (matcher + explicaciones de referencia), D (Supabase + vista asesor + ADR 0003 del esquema). Contratos borrador (`Lead`/`Score`/`LeadCurado`) ya redactados ahí; cada track construye contra fixtures. Únicos puntos de sincronización: scaffold listo (~1h), data sintética publicada (hoy noche), kickoff (ratificar contratos/umbrales), integración del sábado a.m.
- [ ] Limpiar el Excel real antes del motor de scoring: `VLR_VIVIENDA` ÷10.000, normalizar `RANGO_EDAD`/`ETAPA` (dos formatos por valor), decidir qué hacer con los códigos griegos, y resolver la ubicación contradictoria de `VIBO ONCE`/`KARAKALI` entre las hojas del xlsx de brochures.
- [ ] Preguntar a mentores lo que el spec dejó abierto hacia afuera: ¿un lead form de pauta puede pedir cédula?, ¿qué sabe Colsubsidio de un lead que llega por pauta?, convergencia multi-canal, cruces Ministerio/buró.

- [ ] **Ticket [001](../tasks/001-personajes-canonicos.md) — personajes canónicos**, dentro del scaffold. Es el que más bloquea: B siembra esas cédulas, C escribe sus explicaciones sobre esos números, D arma sus fixtures desde ahí.
- [ ] Consolidar ramas: conviven `Track-B` con `feature/scoring` y `mani-TrackC` con `feature/matching`. Acordar una por track antes del primer merge.
- [x] ~~Asignar dueño del ticket [015]~~ — **asignado a Rol 4 (Pitch & Video)** en el grilling 2026-07-24. Guion hoy, grabación el sábado tras la integración (014). Ticket [015](../tasks/015-guion-y-video.md).

### Next (bloqueado hasta que caiga un item de "Now")

- [ ] Ticket [002](../tasks/002-contratos-capacidad-en-score.md) — los dos cambios a `lib/types.ts`. Depende de: kickoff (se ratifican ahí y se anuncian una sola vez).
- [ ] Tickets [003](../tasks/003-enriquecimiento-por-cedula.md) y [004](../tasks/004-capacidad-compartida.md) (viernes a.m.), [005](../tasks/005-agendador.md) · [006](../tasks/006-orquestador.md) · [008](../tasks/008-shell-navegacion.md) (viernes p.m.), [007](../tasks/007-reenganche-nutricion.md) · [014](../tasks/014-recorrido-criterio-4.md) (sábado a.m.). Secuencia completa en [`plan.md §4`](../plan.md).
- [ ] Tests de los criterios de aceptación [011](../tasks/011-test-criterio-1.md) · [012](../tasks/012-test-criterio-2.md) · [013](../tasks/013-test-criterio-3.md) — cada uno se escribe junto con la pieza que verifica, no al final.

### Later (someday / sin scope aún)

- [ ] Decidir si se infiere el mapeo real de los códigos griegos cruzando contra los % del PPT, o si el motor los trata como clusters anónimos.
- [ ] Estrategia de "implementabilidad" (por qué Colsubsidio lo llevaría a producción: CRM Salesforce, canal WhatsApp, cruces inferidos).

### Done

- [x] 2026-07-24 00:49 — **Track D mergeado a `main` (`db137f7`, PR #1) y Supabase creado + poblado.** `/asesor` vive en el link público. Constraints y RLS verificados contra el Postgres real. Falta solo cargar las env vars en Vercel (ver "Now").
- [x] 2026-07-23 22:50 — **Track D: [ADR 0003](../adr/0003-esquema-db-leads.md) (cierra el modelo del plan §2) + SQL para Supabase + `/asesor` completa** (cola priorizada, ficha con score factor por factor, fuente del lead visible, `/api/leads`, `/api/citas`) + **mitades de D de los tickets 005, 007 y 012**. Contra las fixtures canónicas.

- [x] 2026-07-23 — **Scaffold (Fase 1) + Track A completo (Fase 2), mergeados a `main`**: Next.js, `lib/types.ts`, fixtures de los 3 personajes, landing del jurado, chat WhatsApp, lógica adaptativa, `/api/chat` con Claude + streaming y fallback determinístico. Ver Memory arriba para el detalle y los pendientes (Vercel, `/api/leads`).
- [x] 2026-07-23 — **`/plan`**: [`docs/plan.md`](../plan.md) + 15 tickets en [`docs/tasks/`](../tasks/README.md). La ejecución del build se sigue por esos ids.
- [x] 2026-07-23 — **Stack decidido y documentado** ([ADR 0002](../adr/0002-stack-mvp.md)): Next.js + Vercel + Supabase + Claude. Feedback loops y TODOs de `AGENTS.md` llenados.
- [x] 2026-07-23 — [`docs/spec.md`](../spec.md) escrito (7 bloques, 4 criterios de aceptación, 12 supuestos por validar).
- [x] 2026-07-23 — Contrato migrado a `AGENTS.md` + `CLAUDE.md` como puntero + permission floor en `.claude/settings.json`.
- [x] 2026-07-23 — Scaffold del repo de código (este repo): estructura canónica + reorg de los 6 archivos + data fuera de git.
- [x] 2026-07-23 — Scope macro del MVP cerrado ([`docs/mvp-layout.md`](../mvp-layout.md)).
- [x] 2026-07-23 — Reto elegido: Vivienda ([ADR 0001](../adr/0001-eleccion-reto-vivienda.md)).
- [x] 2026-07-23 — Datos reales analizados (Excel 4.142 compradores, PPT buyer personas, brochures).
